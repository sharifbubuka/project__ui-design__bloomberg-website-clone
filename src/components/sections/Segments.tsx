import { FC } from "react";

type Props = {};

const Segments: FC<Props> = () => {
    return (
        <section>
            <div>Segments Section</div>
        </section>
    )
};

export default Segments;
