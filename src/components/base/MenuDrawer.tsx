import { FC } from "react";

type Props = {};

const MenuDrawer: FC<Props> = () => {
    return (
        <section>
            <div>Menu Drawer</div>
        </section>
    )
};

export default MenuDrawer;