import { FC } from "react";

type Props = {};

const Carousel: FC<Props> = () => {
    return (
        <section>
            <div>A Carousel</div>
        </section>
    )
};

export default Carousel;