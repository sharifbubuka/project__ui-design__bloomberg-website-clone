import { FC } from "react";

type Props = {};

const LiveVideoPlayer: FC<Props> = () => {
    return (
        <section>
            <div>Live Video Player</div>
        </section>
    )
};

export default LiveVideoPlayer;